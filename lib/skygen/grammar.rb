class Grammar

  attr_accessor :name, :rules

  def initialize(name,rules)
    @name = name
    @rules = rules
    # @rules = set_rules(rules)
  end

  def set_rules(arr_rules)
    new_rules = []
    arr.each do |rule|
      
    end
  end
end
